<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><h4><%=resource.Heading%></h4></a>
      </li>
    </ul>
    
    <div class="tab-content" id="myTabContent">

        <div class="tab-pane fade in active" id="home" role="tabpanel" aria-labelledby="home-tab">	
            <div style="width:40%">
            <form class="form-horizontal">
                <table class="table table-striped">
                    <tr class="heading-tr">
                        <th> <%=resource.Heading%></th>
                        <th></th>
                    </tr>
                    <tr>
                        <td><%=resource.Subscriber.label%></td>
                        <td><%=record.subscriber%></td>						
                    </tr><tr>
                        <td><%=resource.Account.label%></td>
                        <td><%=record.account%></td>						
                    </tr><tr>
                        <td><%=resource.From.label%></td>
                        <td><%=record.from%></td>						
                    </tr><tr>
                        <td><%=resource.To.label%></td>
                        <td><%=record.to%></td>						
                    </tr><tr>
                        <td><%=resource.Counter.label%></td>
                        <td><%=record.counter%></td>						
                    </tr><tr>
                        <td><%=resource.Type.label%></td>
                        <td><%=record.type%></td>						
                    </tr><tr>
                        <td><%=resource.LastUpdate.label%></td>
                        <td><%=dateFormat(record.last_update,'mmm-dd-yyyy HH:MM:ss')%></td>						
                    </tr><tr>
                        <td><%=resource.Status.label%></td>
                        <td><%=record.status%></td>						
                    </tr>
                
                </table>
                <% if(session.user_acl=='ROLE_ADMIN') { %>
                <button type="submit" class="btn btn-default"><a href="/writeMsbTraffic?subscriber=<%=record.subscriber%>">Edit</a></button>
                <button type="submit" class="btn btn-default"><a href="/deleteMsbTraffic?subscriber=<%=record.subscriber%>">Delete</a></button>
                <% } %>
                <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Back</button>
            </form>
            </div>
        </div>
        
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
