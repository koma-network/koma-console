<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newMsbMucroomMapper">			
	
	<insert id="newMsbMucroom">
		insert into msb_mucroom(roomID,serviceID,creationDate,modificationDate,name,naturalName,description,lockedDate,emptyDate,canChangeSubject,maxUsers,publicRoom,moderated,membersOnly,canInvite,roomPassword,canDiscoverJID,logEnabled,subject,rolesToBroadcast,useReservedNick,canChangeNick,canRegister) 
		values (#{roomid},#{serviceid},#{creationdate},#{modificationdate},#{name},#{naturalname},#{description},#{lockeddate},#{emptydate},#{canchangesubject},#{maxusers},#{publicroom},#{moderated},#{membersonly},#{caninvite},#{roompassword},#{candiscoverjid},#{logenabled},#{subject},#{rolestobroadcast},#{usereservednick},#{canchangenick},#{canregister});
	</insert>

</mapper>
