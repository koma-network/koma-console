<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="readMsbSubscribertypeMapper">			
	
	<select id="readMsbSubscribertype">
		select * from msb_subscribertype where subtype = #{subtype};
	</select>
 
	<select id="readMsbSubscribertypeMsbSubscriber">
        select * from msb_subscriber where subtype = #{subtype};
	</select>
  
</mapper>
